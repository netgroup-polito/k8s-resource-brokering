---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: regionecoforecasts.broker.fluidos.eu
spec:
  group: broker.fluidos.eu
  names:
    kind: RegionEcoForecast
    listKind: RegionEcoForecastList
    plural: regionecoforecasts
    singular: regionecoforecast
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.region
      name: Region
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          RegionEcoForecast is the Schema for the regionecoforecasts API.
          It caches per-region carbon intensity forecast data fetched from the carbon intensity service.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RegionEcoForecastSpec defines the desired state of RegionEcoForecast
            properties:
              carbonIntensity:
                description: CarbonIntensity is the 24-hour carbon intensity forecast
                  in gCO2eq/kWh
                items:
                  type: integer
                type: array
              lastUpdate:
                description: LastUpdate is when this forecast was last fetched from
                  the carbon intensity service
                format: date-time
                type: string
              region:
                description: Region is the region code (e.g. "LOM", "QC", "CA")
                type: string
            required:
            - carbonIntensity
            - lastUpdate
            - region
            type: object
          status:
            description: RegionEcoForecastStatus defines the observed state of RegionEcoForecast
            properties:
              lastUpdateTime:
                description: LastUpdateTime is when this CRD was last reconciled
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
