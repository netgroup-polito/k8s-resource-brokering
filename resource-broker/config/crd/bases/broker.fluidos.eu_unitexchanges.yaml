---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: unitexchanges.broker.fluidos.eu
spec:
  group: broker.fluidos.eu
  names:
    kind: UnitExchanges
    listKind: UnitExchangesList
    plural: unitexchanges
    singular: unitexchanges
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.primaryUnit
      name: PrimaryUnit
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          UnitExchanges is the Schema for the unitexchanges API.
          It caches currency exchange rates for normalizing energy costs.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UnitExchangesSpec defines the desired state of UnitExchanges
            properties:
              lastUpdateUnit:
                description: LastUpdateUnit is when the exchange rates were last refreshed
                format: date-time
                type: string
              primaryUnit:
                description: PrimaryUnit is the target currency (e.g. "EUR/MWh")
                type: string
              rates:
                additionalProperties:
                  type: number
                description: |-
                  Rates maps currency codes to their exchange rate vs the primary unit's base currency
                  e.g. {"USD": 1.08, "GBP": 0.85, "CAD": 1.50, ...}
                  Meaning: 1 EUR = 1.08 USD, 1 EUR = 0.85 GBP, etc.
                type: object
            required:
            - lastUpdateUnit
            - primaryUnit
            - rates
            type: object
          status:
            description: UnitExchangesStatus defines the observed state of UnitExchanges
            properties:
              lastUpdateTime:
                description: LastUpdateTime is when this CRD was last reconciled
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
