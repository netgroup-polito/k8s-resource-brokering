apiVersion: v1
kind: Pod
metadata:
  name: test-offload-nginx
spec:
  tolerations:
  - key: "virtual-node.liqo.io/not-allowed"
    operator: "Exists"
    effect: "NoExecute"
  containers:
  - name: nginx
    image: nginx
